import React from 'react';
import NavBar from '../NavBar/NavBar';

const Header = () => {
    return (
        <div className='h-16'>
            <NavBar></NavBar>
        </div>
    );
};

export default Header;